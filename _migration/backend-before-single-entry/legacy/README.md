# Legacy backend

Port routes/handlers from this directory to Hono in `/backend/api/index.ts`.
Blink runs Hono on Cloudflare Workers (TypeScript only). The original sources are kept here
for reference — do not ship them at runtime.
